# Скопируйте в корень проекта как .env.dokploy (файл в .gitignore, в git не попадёт):
#   cp pocketbase/dokploy.env.example .env.dokploy
# Затем:
#   set -a && source .env.dokploy && set +a && python3 scripts/deploy_pocketbase_dokploy.py

DOKPLOY_URL=https://dokploy.cashbackbrain.ru
DOKPLOY_API_KEY=вставьте_новый_ключ_сюда
